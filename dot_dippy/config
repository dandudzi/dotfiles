allow git add
allow git commit
allow git status
allow git log
allow git diff
allow git show
allow git branch -l
allow git diff
allow wc -l
allow tail
ask git
deny git push --force 
deny git reset --hard 
allow docker ps
allow docker images
allow docker inspect
allow docker logs
ask docker
deny * /etc/* 
deny * /usr/* 
deny * ~/.ssh/* 
allow pnpm test
allow pnpm lint
allow pnpm check-types 
