---
name: fitness-progress
description: Display 10K fitness challenge progress with formatted progress bars
user_invocable: true
---

Run the fitness progress script and display the output to the user:

```bash
bash $HOME/.claude/hooks/fitness-progress.sh
```

Display the output as-is. This is read-only (no state modifications).
