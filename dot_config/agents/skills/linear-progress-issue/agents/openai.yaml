interface:
  display_name: "Progress Linear Issue"
  short_description: "Move a Linear issue safely through its workflow"
  default_prompt: "Use $linear-progress-issue to advance this issue to the next valid workflow state."
