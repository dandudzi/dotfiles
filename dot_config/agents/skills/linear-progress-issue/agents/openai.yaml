interface:
  display_name: "Progress Linear Issue"
  short_description: "Delegate Linear workflow transitions to one subagent"
  default_prompt: "Use $linear-progress-issue to have one medium-effort subagent advance this issue to its next valid workflow state."
