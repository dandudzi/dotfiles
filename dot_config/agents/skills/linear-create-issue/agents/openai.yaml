interface:
  display_name: "Create Linear Issue"
  short_description: "Create a prioritized backlog issue assigned to its project lead"
  default_prompt: "Use $linear-create-issue to create a scoped Backlog issue with explicit priority and the project lead assigned."
