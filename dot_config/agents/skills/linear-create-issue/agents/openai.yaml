interface:
  display_name: "Create Linear Issue"
  short_description: "Create a labeled, prioritized backlog issue for the project lead"
  default_prompt: "Use $linear-create-issue to create a classified Backlog issue with explicit priority and the project lead assigned."
