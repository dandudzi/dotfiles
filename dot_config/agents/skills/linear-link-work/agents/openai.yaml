interface:
  display_name: "Link Work to Linear"
  short_description: "Resolve scoped work, relations, and subissues"
  default_prompt: "Use $linear-link-work to verify the scoped issue, inspect its relations, and propose subissues when needed before handing work to $linear-progress-issue."
