interface:
  display_name: "Coding Workflow"
  short_description: "Deliver changes through a user-approved merge"
  default_prompt: "Use $coding-workflow to implement, verify, and deliver this change through a user-approved pull request merge and linked-ticket closure, delegating only when complexity or risk justifies it."
