Host *
    # Always use SSH2
    Protocol 2

    # multiplex connections: reuse a single TCP/TLS channel per host
#    ControlMaster   auto
  
    # compression and keep-alive settings
    Compression         yes
    TCPKeepAlive        yes
    ServerAliveInterval 20
    ServerAliveCountMax 10
